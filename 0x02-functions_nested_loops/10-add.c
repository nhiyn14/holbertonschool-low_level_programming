#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: number 1
 * @b: number 2
 *
 * Return: Always 0.
 */
int add(int a, int b)
{
	int n = a + b;

	return (n);
}
